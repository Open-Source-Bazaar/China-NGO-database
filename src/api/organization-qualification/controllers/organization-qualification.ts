/**
 * organization-qualification controller
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreController(
  'api::organization-qualification.organization-qualification',
);
